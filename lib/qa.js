export function structuralQa(ast,wordpress,assets=[]){const vd=ast?.visualDom||{},nodes=vd.nodes||[],types=new Map();for(const n of nodes)types.set(n.tag,(types.get(n.tag)||0)+1);const downloaded=assets.filter(a=>a.status==='downloaded').length,failed=assets.filter(a=>a.status==='failed').length,checks={visualNodes:nodes.length,images:types.get('img')||0,links:types.get('a')||0,headings:[1,2,3,4,5,6].reduce((n,x)=>n+(types.get(`h${x}`)||0),0),components:ast?.components?.length||0,sections:ast?.sections?.length||0,assetsDownloaded:downloaded,assetsFailed:failed,hasResponsive:Object.keys(vd.viewports||{}).length>=3,hasThemeJson:!!wordpress?.files?.['theme.json'],hasFrontPage:!!wordpress?.files?.['templates/front-page.html']};let score=100;if(checks.visualNodes<20)score-=35;if(!checks.hasResponsive)score-=15;if(!checks.hasThemeJson||!checks.hasFrontPage)score-=30;if(checks.images&&downloaded===0)score-=15;if(failed)score-=Math.min(15,Math.round(failed/Math.max(1,downloaded+failed)*15));return{status:score>=80?'pass':score>=55?'warning':'fail',score:Math.max(0,score),checks,note:'Structural QA only. Pixel-level comparison requires rendering the generated WordPress theme.'}}
